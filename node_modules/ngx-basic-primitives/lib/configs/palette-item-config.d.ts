import { LineType } from '../enums';
export declare class PaletteItemConfig {
    lineColor: String;
    lineWidth: number;
    lineType: LineType;
    constructor(config?: Partial<PaletteItemConfig>);
}
