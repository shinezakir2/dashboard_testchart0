import { TemplateRef } from '@angular/core';
import AbstractTemplate from './AbstractTemplate';
export declare class CustomRenderTemplate extends AbstractTemplate {
    constructor(options: any, onRender: () => TemplateRef<any>);
}
